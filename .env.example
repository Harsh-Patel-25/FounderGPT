# FounderGPT Frontend Environment Variables (Vite)

# The base URL for the backend API
# If running locally with the default setup, this should be http://localhost:4999
VITE_API_URL=http://localhost:4999

# Note: You must also configure the Backend/.env file with appropriate AI API keys.
